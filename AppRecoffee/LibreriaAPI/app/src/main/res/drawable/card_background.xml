<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Fondo gris oscuro sutil -->
    <solid android:color="#1E1E1E" />

    <!-- Bordes redondeados -->
    <corners android:radius="12dp" />

    <!-- Borde suave -->
    <stroke
        android:width="1dp"
        android:color="#333333" /> <!-- Color de borde un poco más claro que el fondo -->
</shape>
